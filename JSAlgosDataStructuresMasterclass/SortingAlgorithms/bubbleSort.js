const bubbleSort = (arr) => {
  for (var i = arr.length - 1; i >= 0; i--) {
    for (var j = 0; j = i - 1; j++) {
      if (arr[j] > arr[j+1]) {
        // swap values
      }
    }
  }
  return arr;
}
